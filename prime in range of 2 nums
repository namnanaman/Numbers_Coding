// Online C++ compiler to run C++ program online
#include <iostream>
using namespace std;
bool prime(int x){
    int div =0;
    for(int i=1;i<=x;i++ ){
        if(x%i==0){
            div++;
        }
    }
    if(div==2) return true;
    return false;
    
}

int main() {
    int n,m;
    cin>>n>>m;
    for(int y=n;y<=m;y++ ){
        
        if(prime(y)) cout<<y<<" ";
        
    
    }
    

    return 0;
}
